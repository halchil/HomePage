<!DOCTYPE html>

<html lang="en">

<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="/css/styles.css" rel="stylesheet">
</head>

<body class="bg-gray-100 text-gray-800">

  <div class="bg-gray-100 flex">
        
        <!-- 縦帯の画像 -->
        <div class="bg-contain bg-center w-64 h-screen" style="background-image: url('/images/name.png');">
        <!-- 必要に応じて画像タグでもOK -->
        <!-- <img src="/images/sidebar.jpg" class="h-full w-full object-cover" alt="Sidebar"> -->
        </div>

        <!-- 右側 -->
        <div class="bg-gray-200 flex-1 p-4">
        <%- include('partials/header') %> <!-- ここでヘッダーを表示 -->
        
            <div class="mt-10 space-y-8">

              <h1 class="text-4xl font-bold text-gray-700 mb-2 pt-5">Profile</h1>

              <div class="relative w-full h-auto bg-blue-50 p-2 rounded-lg shadow-sm">
              
                <!-- アイコン -->
                  <div class="flex items-center space-x-4 mb-6">
                    
                    <div class="w-16 h-16">
                      <img src="/images/k8s.png" alt="アイコン" class="w-full h-full object-contain" />
                    </div>

                      <div>
                        <p class="text-xl font-semibold py-5">感情迷子ナビゲーター ばさ美</p>
                        <p class="text-gray-600">
                        どうも。ばさ美よ。<p>
                        恋愛や日常生活で感じるモヤモヤ、ちょっとした悩みや不安を丁寧に聞いているわ。<p>  
                        どんなささいで小さなことでも話してちょうだい。 <p>
                        あなたの気持ちに寄り添いながら、心が軽くなるヒントや前向きになれるアドバイスをお届けしちゃうわよ。<p>
                      </p>
                    </div>
                  </div>
              </div>

            </div>

            <!-- 例えばプロフィールの下あたりに -->

            <div class="mt-10 space-y-8">
              <h2 class="text-3xl font-bold mb-4">お悩み相談リスト</h2>

              <% if (adviceList.length === 0) { %>
                <p>No advice available.</p>
              <% } else { %>
                <ul class="space-y-4">
                  <% adviceList.forEach(advice => { %>
                    <li class="bg-white p-4 rounded shadow">
                      <p class="text-sm text-gray-500"><%= advice.date %></p>
                      <h3 class="text-xl font-semibold text-blue-600"><a href="<%= advice.note_url %>" target="_blank" rel="noopener noreferrer"><%= advice.title %></a></h3>
                      <p class="mt-1 text-gray-700">Tags: 
                        <% advice.tags.forEach((tag, index) => { %>
                          <span class="inline-block bg-gray-200 rounded px-2 py-1 text-xs mr-2"><%= tag %></span>
                        <% }) %>
                      </p>
                    </li>
                  <% }) %>
                </ul>
              <% } %>
            </div>

         </div> <!-- 右側 -->
         
    </div>

</body>
</html>