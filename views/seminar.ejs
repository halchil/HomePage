<!DOCTYPE html>

<html lang="en">

<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="/css/styles.css" rel="stylesheet">
</head>

<body class="bg-gray-100 text-gray-800">

    <div class="bg-gray-100 flex">
        
        <!-- 縦帯の画像 -->
        <div class="bg-contain bg-center w-64 h-screen" style="background-image: url('/images/name.png');">
        <!-- 必要に応じて画像タグでもOK -->
        <!-- <img src="/images/sidebar.jpg" class="h-full w-full object-cover" alt="Sidebar"> -->
        </div>

        <!-- 右側 -->
        <div class="bg-gray-200 flex-1 p-4">
        <%- include('partials/header') %> <!-- ここでヘッダーを表示 -->
        
            <div class="mt-10 space-y-8 p-10">
              <div class="grid grid-cols-2 grid-rows-2 gap-8 p-6">
                <% seminars.forEach(seminar => { %>
                  <div class="bg-white p-6 rounded-lg shadow-md flex flex-col items-center">
                    <a href="https://www.youtube.com/watch?v=<%= seminar.id %>&t=<%= seminar.startTime %>" target="_blank" rel="noopener noreferrer" class="mb-4">
                      <img src="https://img.youtube.com/vi/<%= seminar.id %>/hqdefault.jpg" alt="<%= seminar.title %> Thumbnail" class="rounded-lg shadow-sm" />
                    </a>
                    <a href="https://www.youtube.com/watch?v=<%= seminar.id %>&t=<%= seminar.startTime %>" target="_blank" rel="noopener noreferrer" class="text-lg font-semibold hover:text-blue-600">
                      <%= seminar.title %>
                    </a>
                    <p class="text-gray-600 text-sm mt-2"><%= seminar.description %></p>
                    <p class="text-gray-500 text-xs mt-1"><%= seminar.date %></p>
                  </div>
                <% }) %>
              </div>
            </div>
   
         </div>
         
    </div>

</body>
</html>